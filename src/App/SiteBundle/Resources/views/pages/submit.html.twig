{% extends 'SiteBundle:layouts:master.http.twig' %}
{% block content %}
<div class="panel">
    <h1>Подтверждение удаления</h1>
</div>

    <div class="panel-heading">

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>


            <tr>
                <th>Name</th>
                <td>{{ post.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ post.email }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ post.subject }}</td>
            </tr>


        </tbody>
    </table>
        </div>


            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" align="right" class="btn-primary">
            {{ form_end(delete_form) }}

{% endblock %}

